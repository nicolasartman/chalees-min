# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
node_modules

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Darnit OS X, clean up your junk!
.DS_Store

# the published files that get uploaded to the static host. The js files are autogenerated
# so they shouldn't be version controlled.
dist

# compiled firebase security rules -- the source is kept in the yaml file,
# and it only needs to be compiled for firebase-tools to upload it
security-rules.json

# local development configuration file
dev.config.js

# Gif diff leftovers
*.orig

# Elm and build stuff
dist
elm-stuff
