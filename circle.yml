machine:
  node:
general:
  branches:
    only:
      - master
deployment:
  staging:
    branch: master
    commands:
      - npm run ci-deploy-staging
  # production:
  #   branch: master
  #   commands:
  #     - npm run ci-deploy-production
