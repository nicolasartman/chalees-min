machine:
  node:
    version: 6.1.0
deployment:
  staging:
    branch: master
    commands:
      - npm run ci-deploy-staging
  # production:
  #   branch: master
  #   commands:
  #     - npm run ci-deploy-production
  
